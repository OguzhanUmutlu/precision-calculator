{
  "name": "precision-calculator",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "deploy": "vite build && gh-pages -d dist && node -e \"require('fs').rmSync('./dist',{recursive:true})\""
  },
  "dependencies": {
    "bignumber.js": "^9.1.2",
    "decimal.js": "^10.4.3",
    "fraction.js": "^4.3.7",
    "gh-pages": "^6.0.0",
    "vite": "^5.0.2"
  }
}
